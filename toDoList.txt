we are current getting the prompt generation up and running with the button at the end of each chapter section.
we want to mauyve use a pre tag or a button tag to give the button text to what we want the ai do generate. we want this to be pretty dynamic



this is the function weve used to get the current context. but we still need to scrape the ToC and the addiont context for the AI

 function makePrompt(evt){
  if(evt){
    console.log(evt.target)
    let elem = evt.target
    const html=[]
    while(elem.tagName!=="H2"){
      elem = elem.previousElementSibling
      if(elem.tagName==="DIV" && elem.className==="monaco"){
        continue
      }
      html.unshift(elem.outerHTML)
    }
    console.log("fount it:", elem.tagName) 
    console.log(html)
    const turndownService = new TurndownService();

    console.log(turndownService.turndown(html.join("")))
  }

}



